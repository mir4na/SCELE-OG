# Copy this file to `.env` and adjust values for your local/deploy setup.

# Shared secret between LMS and bot internal endpoints.
BOT_SHARED_SECRET=change-this-secret

# Optional fixed grade token. Leave empty to auto-generate random token on LMS startup.
GRADE_TOKEN=

# Optional bot review interval (ms).
BOT_REVIEW_INTERVAL_MS=7000
